# 🥬 next-ingredient

Reserved for the next slice of the Sandwich.

This is a placeholder — an empty plate. The Sandwich grows one ingredient at a
time, and each one ships and proves its worth on its own before the next is
added. When a real ingredient lands here (a Build slice, a Review slice, a QA
slice, a client Handover slice…), it replaces this folder.

Rule of the kitchen: don't add an ingredient until someone's actually hungry for
it. No speculative layers.

## Installation

Nothing to install — this is an empty plate. When a real ingredient lands here it
will ship with its own installation manual in this README, following the same
shape as [`../breakdown/README.md`](../breakdown/README.md) (prerequisites →
per-harness steps → verify).

See the top-level `README.md` for the full menu.
